database:
  image: postgres:9.5


authservice:
  build: .
  links:
    - database

nginx:
  image: nginx:latest
  volumes:
    - ./index.htm:/www/index.htm
    - ./nginx.conf:/etc/nginx/nginx.conf
  ports:
    - 8000:8000
  links:
    - authservice
