# Using auth-service in the backend

When writing a backend service you will want to access information about the
authenticated user. To enable this, auth-service provides a library,
`auth-service-core`.

## Using the auth-service
